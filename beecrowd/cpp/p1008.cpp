#include <bits/stdc++.h>

using namespace std;

int main()
{

    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, w;
    double p;
    cin >> n >> w >> p;
    cout << "NUMBER = " << n << '\n';
    cout << fixed << setprecision(2) << "SALARY = U$ " << w * p << '\n';

    return 0;
}
